# Legacy Streamlit Demo

Aplicativo de exemplo em Streamlit, anterior ao projeto AutoPrime. Mantido aqui apenas
para referência histórica. O desenvolvimento atual está em `autoprime/`.

## Rodando

```bash
cd legacy-streamlit-app
pip install -r requirements.txt
streamlit run app.py
```
